const version = '1.0.0';
export const header = {
    css: `/**
   * Built with DyeScript v${version}
   * by Yazid Slila (@yokgs)
   */`,
    cssmin: `/** 
   * Built with DyeScript v${version}
   * by Yazid Slila (@yokgs) 
   * */\n`,
    reactnative: `/** 
   * Built with DyeScript v${version}
   * by Yazid Slila (@yokgs)
   */\nimport { StyleSheet } from "react-native";\nexport default StyleSheet.create({ `,
    staticdye: `!! DyeScript Swift File\n!version ${version}`
};
